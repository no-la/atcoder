#include <bits/stdc++.h>
using namespace std;

using ll = long long;

ll N, M, a[100000], b[100000];

int main(){
    cin >> N >> M;
    for (int i=0; i<N; i++) {
        cin >> a[i] >> b[i];
    }

    return 0;
}